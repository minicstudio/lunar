# Localities Plugin

The Lunar Localities plugin provides reusable `County` and `Locality` models for locality-related workflows across packages.

## Included Models

- `Lunar\\Localities\\Models\\County`
- `Lunar\\Localities\\Models\\Locality`

## Notes

- Uses existing `counties` and `localities` tables.
- Registers model classes through `ModelManifest`.
