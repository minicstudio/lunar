## 2026-08-21

- Fixed admin attribute save for translated fields (e.g. collection meta keywords/description): Filament locale arrays are converted into FieldType instances with values preserved, instead of calling `get_class()` on arrays and dropping entered text.
