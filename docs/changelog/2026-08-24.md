## 2026-08-24

- Fixed admin product save emptying translated attributes (description, short description, etc). Reverted the LFP-786 change that registered locale editors via Filament `childComponents()`, which caused save to dehydrate those editors as empty and overwrite the parent value. Restored the previous local `$components` wiring used only by the translated-text view.
