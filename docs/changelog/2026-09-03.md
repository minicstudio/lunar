## 2026-09-03

- Loyalty order earn/spend relations register on `Lunar\Models\Order` (not `Order::modelClass()` at boot). Storefront Order subclasses inherit them; no duplicate registration in lunar-frontend.
